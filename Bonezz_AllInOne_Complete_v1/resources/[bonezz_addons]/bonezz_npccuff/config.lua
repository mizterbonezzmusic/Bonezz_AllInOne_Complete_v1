Config = {}

-- Who can cuff (job names lowercased). Leave empty to allow everyone.
Config.AllowedJobs = { 'police', 'sheriff', 'state', 'lspd', 'bcso' }

-- Distance to search for NPCs
Config.SearchRadius = 2.5

-- Enable qb-target interaction on peds (if qb-target installed)
Config.EnableTarget = true
